{
  "name": "counter-predicate-client",
  "version": "1.0.0",
  "description": "TypeScript client for interacting with Counter contract using Predicate signatures",
  "main": "increment-counter.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/increment-counter.js",
    "dev": "ts-node increment-counter.ts",
    "increment": "ts-node increment-counter.ts"
  },
  "dependencies": {
    "ethers": "^6.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 